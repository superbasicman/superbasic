{
  "buildCommand": "cd ../.. && turbo run build --filter=@repo/api",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/api packages/auth packages/database packages/types packages/observability packages/rate-limit && exit 1 || exit 0"
}
