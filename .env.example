# Root Environment Variables
# Copy this file to .env and fill in your actual values

# Node Environment
NODE_ENV=development

# Database (shared across packages and API)
DATABASE_URL=postgresql://user:password@localhost:5432/superbasic_finance_dev

# API Configuration (see apps/api/.env.example for API-specific vars)
# PORT=3000

# Web Client Configuration (see apps/web/.env.example for web-specific vars)
# VITE_API_URL=http://localhost:3000

# Note: Each app and package may have its own .env.example file
# Make sure to copy and configure those as well:
# - apps/api/.env.example
# - apps/web/.env.example
# - packages/database/.env.example
