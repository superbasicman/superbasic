# Database connection string for Postgres
# For local development: Use your Neon 'dev' branch connection string
# For production: Set in Vercel environment variables pointing to 'main' branch
# Get connection strings from: Neon Dashboard > Branches > [branch name] > Connection Details
#
# Neon format (recommended):
# DATABASE_URL="postgresql://user:password@ep-branch-name-pooler.region.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
#
# Local Postgres format (alternative):
# DATABASE_URL="postgresql://postgres:password@localhost:5432/superbasic_dev?schema=public"
DATABASE_URL="postgresql://user:password@ep-dev-branch-pooler.region.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Production main branch URL (for running migrations via tooling/scripts/migrate-production.ts)
# Get from: https://console.neon.tech → Your Project → Branches → main
NEON_MAIN_URL="postgresql://user:password@ep-main-branch-pooler.region.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
